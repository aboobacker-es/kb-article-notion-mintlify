---
title: "HackerRank Documentation"
description: "Ask HackerRank Docs anything, powered by AI. Every answer includes citations to the exact article."
mode: "custom"
---

<style dangerouslySetInnerHTML={{__html: `
  /* Global: no left sidebar, no search anywhere. */
  #sidebar,
  nav[aria-label="Pages"],
  [data-testid*="search" i],
  button[aria-label*="search" i],
  .search-trigger { display: none !important; }

  /* Landing page only ("/"). Mintlify tags the current path on <html>. */
  html[data-current-path="/"] {
    /* The assistant's sheet parent uses this variable for its width. */
    --assistant-sheet-width: 100vw !important;
    --content-max-width: 100vw !important;
  }
  /* Hide the docs content column on landing so the assistant fills the screen. */
  html[data-current-path="/"] #content-area,
  html[data-current-path="/"] main .mdx-content { display: none !important; }
  /* Kill the flex-child that normally holds the content area, so its parent doesn't reserve space. */
  html[data-current-path="/"] div.lg\\:flex-1.lg\\:min-w-0 { display: none !important; }

  /* Make the assistant sheet fill the viewport and centre its inner column. */
  html[data-current-path="/"] #chat-assistant-sheet {
    width: 100vw !important;
    max-width: 100vw !important;
  }
  html[data-current-path="/"] #chat-assistant-sheet > * {
    max-width: 860px !important;
    margin-left: auto !important;
    margin-right: auto !important;
    width: 100% !important;
  }
`}} />

<script dangerouslySetInnerHTML={{__html: `
  (function() {
    if (typeof window === 'undefined') return;
    if (window.location.pathname !== '/') return;
    // Ensure the assistant opens on landing.
    if (!/[?&]assistant\\b/.test(window.location.search)) {
      window.history.replaceState(null, '', '/?assistant');
    }
    var opened = false;
    var openPanel = function() {
      if (opened) return;
      // Setting the state attribute is the most reliable trigger.
      var html = document.documentElement;
      if (html.getAttribute('data-assistant-state') === 'closed') {
        html.setAttribute('data-assistant-state', 'open');
      }
      var btn = document.getElementById('assistant-entry-mobile')
        || document.querySelector('button[aria-label*="ssistant" i]')
        || document.querySelector('a[href="/?assistant"]');
      if (btn) { btn.click(); opened = true; }
    };
    var kick = function() {
      setTimeout(openPanel, 200);
      setTimeout(openPanel, 700);
      setTimeout(openPanel, 1500);
    };
    if (document.readyState === 'complete') kick();
    else window.addEventListener('load', kick);
  })();
`}} />
