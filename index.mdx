---
title: "How can we help?"
description: "AI-powered documentation for HackerRank. Ask a question to get instant answers with citations."
mode: "custom"
---

<style dangerouslySetInnerHTML={{__html: `
  #sidebar, nav[aria-label="Pages"] { display: none !important; }
  main, [class*="lg:pl-72"] { padding-left: 0 !important; margin-left: 0 !important; }
  [data-testid*="search" i], button[aria-label*="search" i], .search-trigger { display: none !important; }
`}} />

<div className="min-h-[60vh] flex flex-col items-center justify-center text-center px-6 py-16">
  <h1 className="text-4xl md:text-5xl font-semibold tracking-tight mb-4">
    Ask HackerRank Docs anything
  </h1>
  <p className="text-lg text-gray-600 dark:text-gray-300 max-w-xl mb-8">
    Get instant answers powered by AI. Every answer includes citations to the exact article.
  </p>
  <p className="text-sm text-gray-500">
    Press <kbd className="px-2 py-1 border rounded">⌘</kbd> + <kbd className="px-2 py-1 border rounded">I</kbd> or click the button in the corner to start.
  </p>
</div>

<script
  dangerouslySetInnerHTML={{
    __html: `
      (function() {
        if (typeof window === 'undefined') return;
        if (window.location.pathname !== '/') return;
        var open = function() {
          var trigger = document.querySelector('[data-assistant-trigger]')
            || document.querySelector('button[aria-label*="ssistant" i]')
            || document.querySelector('button[aria-label*="AI" i]');
          if (trigger) trigger.click();
        };
        if (document.readyState === 'complete') setTimeout(open, 400);
        else window.addEventListener('load', function () { setTimeout(open, 400); });
      })();
    `
  }}
/>
